What if we want to store the results of a query.
What if we want to query the results of a query.


Views allow us to store and query previously run queries.

TYPES OF VIEWS: 
	1. Materialized - Stores the data physically and prediocally updates it when there is a change.
	2. Non-Materialized - Query gets re-run each time the view is called.


